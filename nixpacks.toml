# Nixpacks configuration for Railway
# Force Node.js 20 for compatibility with @octokit, @supabase, and other packages

[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate"]
